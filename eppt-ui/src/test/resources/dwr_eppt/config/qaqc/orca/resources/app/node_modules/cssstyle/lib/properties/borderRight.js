'use strict';

var shorthandSetter = require('cssstyle/lib/parsers').shorthandSetter;
var shorthandGetter = require('cssstyle/lib/parsers').shorthandGetter;
var shorthandParser = require('cssstyle/lib/parsers').shorthandParser;

var shorthand_for = {
    'border-right-width': require('cssstyle/lib/properties/borderRightWidth'),
    'border-right-style': require('cssstyle/lib/properties/borderRightStyle'),
    'border-right-color': require('cssstyle/lib/properties/borderRightColor')
};

var isValid = function isValid(v) {
    return shorthandParser(v, shorthand_for) !== undefined;
};
module.exports.isValid = isValid;

module.exports.definition = {
    set: shorthandSetter('border-right', shorthand_for),
    get: shorthandGetter('border-right', shorthand_for),
    enumerable: true,
    configurable: true
};
