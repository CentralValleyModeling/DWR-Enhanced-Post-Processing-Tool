'use strict';

var shorthandParser = require('cssstyle/lib/parsers').shorthandParser;
var shorthandSetter = require('cssstyle/lib/parsers').shorthandSetter;
var shorthandGetter = require('cssstyle/lib/parsers').shorthandGetter;

var shorthand_for = {
    'background-color': require('cssstyle/lib/properties/backgroundColor'),
    'background-image': require('cssstyle/lib/properties/backgroundImage'),
    'background-repeat': require('cssstyle/lib/properties/backgroundRepeat'),
    'background-attachment': require('cssstyle/lib/properties/backgroundAttachment'),
    'background-position': require('cssstyle/lib/properties/backgroundPosition')
};

module.exports.isValid = function isValid(v) {
    return shorthandParser(v, shorthand_for) !== undefined;
};

module.exports.definition = {
    set: shorthandSetter('background', shorthand_for),
    get: shorthandGetter('background', shorthand_for),
    enumerable: true,
    configurable: true
};
