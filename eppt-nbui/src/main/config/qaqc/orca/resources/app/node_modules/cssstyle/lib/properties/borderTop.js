'use strict';

var shorthandSetter = require('cssstyle/lib/parsers').shorthandSetter;
var shorthandGetter = require('cssstyle/lib/parsers').shorthandGetter;
var shorthandParser = require('cssstyle/lib/parsers').shorthandParser;

var shorthand_for = {
    'border-top-width': require('cssstyle/lib/properties/borderTopWidth'),
    'border-top-style': require('cssstyle/lib/properties/borderTopStyle'),
    'border-top-color': require('cssstyle/lib/properties/borderTopColor')
};

module.exports.isValid = function (v) {
    return shorthandParser(v, shorthand_for) !== undefined;
};

module.exports.definition = {
    set: shorthandSetter('border-top', shorthand_for),
    get: shorthandGetter('border-top', shorthand_for),
    enumerable: true,
    configurable: true
};
