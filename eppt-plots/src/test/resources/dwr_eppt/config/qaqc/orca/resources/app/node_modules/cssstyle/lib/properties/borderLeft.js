'use strict';

var shorthandSetter = require('cssstyle/lib/parsers').shorthandSetter;
var shorthandGetter = require('cssstyle/lib/parsers').shorthandGetter;
var shorthandParser = require('cssstyle/lib/parsers').shorthandParser;

var shorthand_for = {
    'border-left-width': require('cssstyle/lib/properties/borderLeftWidth'),
    'border-left-style': require('cssstyle/lib/properties/borderLeftStyle'),
    'border-left-color': require('cssstyle/lib/properties/borderLeftColor')
};

var isValid = function isValid(v) {
    return shorthandParser(v, shorthand_for) !== undefined;
};
module.exports.isValid = isValid;

module.exports.definition = {
    set: shorthandSetter('border-left', shorthand_for),
    get: shorthandGetter('border-left', shorthand_for),
    enumerable: true,
    configurable: true
};
