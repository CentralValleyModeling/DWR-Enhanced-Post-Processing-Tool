'use strict';

var shorthandSetter = require('cssstyle/lib/parsers').shorthandSetter;
var shorthandGetter = require('cssstyle/lib/parsers').shorthandGetter;
var shorthandParser = require('cssstyle/lib/parsers').shorthandParser;

var shorthand_for = {
    'border-bottom-width': require('cssstyle/lib/properties/borderBottomWidth'),
    'border-bottom-style': require('cssstyle/lib/properties/borderBottomStyle'),
    'border-bottom-color': require('cssstyle/lib/properties/borderBottomColor')
};

var isValid = function isValid(v) {
    return shorthandParser(v, shorthand_for) !== undefined;
};
module.exports.isValid = isValid;

module.exports.definition = {
    set: shorthandSetter('border-bottom', shorthand_for),
    get: shorthandGetter('border-bottom', shorthand_for),
    enumerable: true,
    configurable: true
};
